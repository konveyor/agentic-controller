# agent-base: Base image for all agent workloads.
#
# Contains the harness entrypoint, skillctl, and system tools.
# The harness manages git lifecycle and launches the agent runtime.
# See: https://github.com/konveyor/enhancements/pull/296
#
# TODO: This is a placeholder. The actual harness binary and tooling
# will be added as part of Stream 4 (Base Image).

FROM registry.access.redhat.com/ubi9/ubi-minimal:latest

# Create the skills directory where ImageVolumes are mounted.
RUN mkdir -p /opt/skills /workspace

# TODO: Install harness binary
# COPY konveyor-harness /usr/local/bin/konveyor-harness

# TODO: Install skillctl
# COPY skillctl /usr/local/bin/skillctl

# TODO: Install system tools (git, etc.)
# RUN microdnf install -y git && microdnf clean all

USER 65532:65532
WORKDIR /workspace

# TODO: ENTRYPOINT ["/usr/local/bin/konveyor-harness"]
