kind: bugfix
description: >
  Gateway connectivity verification now sends Anthropic's required
  x-api-key and anthropic-version headers instead of Authorization: Bearer,
  so Anthropic gateways can reach ConnectionVerified; unrecognized providers
  fall back to the OpenAI-compatible probe and are logged.
