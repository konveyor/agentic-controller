kind: feature

description: >
  Support multi-variable LLM provider credentials (e.g. AWS Bedrock SigV4):
  when credentialRef omits key, the whole credential Secret is exposed to
  the sandbox container via envFrom instead of a single
  KONVEYOR_MODEL_<ROLE>_API_KEY variable.
